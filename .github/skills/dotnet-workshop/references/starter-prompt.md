# Starter Prompt

Use this prompt to test the .NET workshop setup.

```text
Design a .NET web API with a separate test project, generate the project structure, explain the build and test commands, document the architecture in Markdown, and add a Mermaid dependency diagram.
```