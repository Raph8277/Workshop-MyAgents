# Starter Prompt

Use this prompt to test the SQLite workshop setup.

```text
Design a SQLite database for a task tracking application, generate the schema SQL, add seed data, explain how to build the .db file, propose a first migration, document the tables in Markdown, and add a Mermaid ER diagram.
```