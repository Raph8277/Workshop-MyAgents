# Review Checklist

- Is the base branch explicit?
- Is the feature branch name explicit?
- Are local branch creation and `origin` publication both covered?
- Is the PR mode clearly identified as merge or rebase?
- Are the commands ordered and safe?
- If rebase is used, is the history rewrite risk documented?
- If a Mermaid diagram is included, does it match the written workflow?
